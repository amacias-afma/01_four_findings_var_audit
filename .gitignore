# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
.python-version

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Project specific
data/
# ...but the frozen price snapshots MUST be committed: they are what makes every
# reported number reproducible. Do not remove these negations.
!data/snapshots/
!data/snapshots/**
*.log

# Superseded pipeline + stale results, kept for reference only
_archive/

# LaTeX build artefacts — Overleaf los regenera en cada compilación.
# Reglas por EXTENSIÓN, no por nombre: una versión anterior listaba main.pdf y
# four-findings-*.pdf y se le escaparon v2named.* y build-named.*
paper/tex/*.aux
paper/tex/*.bbl
paper/tex/*.blg
paper/tex/*.fdb_latexmk
paper/tex/*.fls
paper/tex/*.log
paper/tex/*.out
paper/tex/*.synctex.gz
paper/tex/*.toc
paper/tex/*.pdf
paper/tex/anon.tex
paper/tex/anon.flag

/main.aux
/main.bbl
/main.blg
/main.out
/main.pdf
/main.fls
/main.fdb_latexmk
/main.synctex.gz
# Root-level LaTeX artefacts.
# main.tex lives at the repo root so Overleaf finds it; its build products must not be
# tracked. Overleaf regenerates every one on each compile, and a tracked copy is the most
# common cause of a GitHub link that "sees" the repo and then refuses to sync.
/main.aux
/main.bbl
/main.blg
/main.fdb_latexmk
/main.fls
/main.log
/main.out
/main.pdf
/main.synctex.gz
/main.toc
